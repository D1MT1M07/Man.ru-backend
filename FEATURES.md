# 🎯 Функциональность man.ru - Руководство пользователя

## 📋 Содержание
1. [Фильтрация по категориям](#фильтрация-по-категориям)
2. [Добавление статей](#добавление-статей)
3. [Форум (обсуждения)](#форум-обсуждения)
4. [Система сохранения данных](#система-сохранения-данных)
5. [Технические подробности](#технические-подробности)

---

## 🏷️ Фильтрация по категориям

### Как это работает

На сайте реализована система категоризации статей. В навигации доступны 6 категорий:

- **Здоровье** - статьи о здоровье и медицине
- **Фитнес** - тренировки, спорт, упражнения
- **Карьера** - профессиональное развитие, работа
- **Технологии** - новинки техники, гаджеты
- **Стиль** - мода, внешний вид, имидж
- **Отношения** - советы об отношениях

### Как использовать

1. **Переход к категории:**
   - Нажмите на название категории в навигации (например, "Фитнес")
   - Статьи на странице автоматически отфильтруются только по этой категории
   - Кнопка категории будет подсвечена как активная (голубой фон)
   - Появится информационное сообщение о применённом фильтре

2. **Показать все статьи:**
   - Нажмите на логотип "man.ru" в левом верхнем углу
   - Все фильтры будут сброшены
   - Снова отобразятся все статьи со всех категорий

3. **Что происходит при фильтрации:**
   - Система ищет все статьи с выбранной категорией
   - Отображает только найденные статьи в сетке
   - Статьи сортируются по дате добавления (новые выше)
   - Если нет статей в категории - показывается сообщение о пустой категории

---

## ✍️ Добавление статей

### Как добавить новую статью

1. **Откройте форму добавления:**
   - Найдите кнопку "📝 Добавить статью" в навигации (мобильное меню)
   - Или нажмите на соответствующий пункт меню
   - Откроется модальное окно с формой

2. **Заполните форму:**
   - **Заголовок** - название вашей статьи (обязательно)
   - **Категория** - выберите подходящую категорию из выпадающего списка (обязательно)
   - **Описание** - краткое содержание или аннотация статьи (обязательно)
   - **Автор** - ваше имя (необязательно, по умолчанию "Аноним")

3. **Опубликуйте статью:**
   - Нажмите кнопку "Добавить статью"
   - Форма закроется и появится зелёное уведомление ✅
   - Новая статья тут же добавится в список

### Правила добавления статей

- **Минимальные требования:** заголовок, категория, описание
- **Размер описания:** от 10 до 500 символов рекомендуется
- **Язык:** русский язык
- **Уникальность:** каждой статье присваивается автоматический ID и дата

### Где найти добавленные статьи

- Новые статьи добавляются в конец списка
- Они сохраняются в памяти браузера (localStorage)
- Статьи остаются даже после закрытия сайта
- Могут быть просмотрены через фильтр по категориям

---

## 💬 Форум (обсуждения)

### Что такое форум

Форум - это раздел, где пользователи могут создавать темы для обсуждения и делиться мнениями по различным вопросам.

### Как получить доступ к форуму

1. **Откройте форум:**
   - Найдите кнопку "💬 Форум" в навигации
   - Нажмите на неё
   - Откроется модальное окно с форумом

### Как создать новую тему

1. **Форма создания:**
   - Вверху модального окна находится форма "Добавить новую тему для обсуждения"
   - Заполните следующие поля:

   | Поле | Описание | Обязательно |
   |------|---------|------------|
   | Название темы | Название для обсуждения | ✅ Да |
   | Раздел | Категория обсуждения | ✅ Да |
   | Сообщение | Текст вашего сообщения | ✅ Да |
   | Имя | Ваше имя/никнейм | ❌ Нет |

2. **Опубликуйте тему:**
   - Нажмите "Создать тему"
   - Новая тема появится в списке обсуждений выше
   - Появится уведомление об успехе ✅

### Как просматривать обсуждения

1. **Список тем:**
   - Ниже формы добавления находится раздел "Обсуждения на форуме"
   - Показаны все активные темы

2. **Информация о теме:**
   - **Название** - заголовок темы
   - **Категория** - раздел обсуждения
   - **Автор** - кто создал тему
   - **Дата** - когда была создана тема
   - **Комментарии** - количество комментариев (в будущей версии)

3. **Удаление темы:**
   - Нажмите кнопку "Удалить" возле нужной темы
   - Потребуется подтверждение
   - Тема будет удалена из списка

### Категории форума

Форум использует те же категории, что и основной сайт:
- Здоровье
- Фитнес
- Карьера
- Технологии
- Стиль
- Отношения

---

## 💾 Система сохранения данных

### Как работает сохранение

Все данные (статьи и форум-посты) сохраняются в **localStorage браузера**:

- **Что это такое:** локальное хранилище браузера на вашем компьютере
- **Сколько хранится:** данные остаются даже после закрытия сайта
- **Где находятся:** только на вашем компьютере, никуда не передаются
- **Объём:** можно хранить примерно 5-10 МБ данных на один сайт

### Ключи хранилища

| Ключ | Описание |
|------|---------|
| `man_ru_articles` | Все статьи (JSON-массив) |
| `man_ru_forum_posts` | Все форум-посты (JSON-массив) |

### Как очистить данные

**Через инструменты разработчика (F12):**
```javascript
// Очистить все данные
localStorage.clear();

// Или конкретные данные
localStorage.removeItem('man_ru_articles');
localStorage.removeItem('man_ru_forum_posts');
```

**Через браузер:**
1. Откройте параметры браузера
2. История → Очистить историю
3. Отметьте "Файлы cookie и данные веб-сайтов"
4. Нажмите "Очистить"

### Экспорт/Импорт данных

**Экспорт (сохранить на компьютер):**
```javascript
const articles = JSON.parse(localStorage.getItem('man_ru_articles'));
const posts = JSON.parse(localStorage.getItem('man_ru_forum_posts'));
const data = { articles, posts };
console.log(JSON.stringify(data, null, 2));
```

---

## 🔧 Технические подробности

### Архитектура системы

```
┌─────────────────────────────────────────┐
│          Пользовательский интерфейс      │
│  (HTML - index.html)                    │
└────────────────┬────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────┐
│  Слой представления (Renderer)          │
│  - Отображение статей                   │
│  - Отображение форум-постов             │
│  - Маппинг категорий                    │
└────────────────┬────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────┐
│  Слой управления (DataManager)          │
│  - CRUD операции                        │
│  - Фильтрация по категориям             │
│  - Работа с localStorage                │
└────────────────┬────────────────────────┘
                 │
                 ↓
┌─────────────────────────────────────────┐
│  Хранилище данных (localStorage)        │
│  - man_ru_articles                      │
│  - man_ru_forum_posts                   │
└─────────────────────────────────────────┘
```

### Классы JavaScript

#### DataManager
Управление всеми данными сайта:

```javascript
const dm = new DataManager();

// Работа со статьями
dm.getArticles()                    // Получить все статьи
dm.addArticle(article)              // Добавить статью
dm.getArticlesByCategory(cat)       // Получить по категории
dm.getArticlesByFilter(keyword)     // Поиск по ключевому слову

// Работа с форумом
dm.getForumPosts()                  // Получить все посты
dm.addForumPost(post)               // Добавить пост
dm.deleteForumPost(id)              // Удалить пост
dm.getForumPostsByCategory(cat)     // Получить по категории
dm.getAllForumPosts()               // Получить все (отсортировано)
```

#### Renderer
Динамическое отображение содержимого:

```javascript
// Отображение
Renderer.renderArticles(articles)   // Отобразить статьи
Renderer.renderForumPosts(posts)    // Отобразить форум-посты
Renderer.getCategoryName(code)      // Преобразовать код в название
```

#### Modal
Управление модальными окнами:

```javascript
Modal.open(modalId)                 // Открыть модальное окно
Modal.close(modalId)                // Закрыть модальное окно
Modal.setupCloseHandlers()          // Установить обработчики закрытия
```

### Структура данных статьи

```json
{
  "id": 1,
  "title": "Заголовок статьи",
  "category": "fitness",
  "description": "Описание статьи",
  "author": "Имя автора",
  "date": "14 января",
  "views": "2.4K"
}
```

### Структура данных форум-поста

```json
{
  "id": 1,
  "title": "Название темы",
  "category": "career",
  "message": "Текст сообщения",
  "author": "Имя автора",
  "date": "2024-01-14 14:30:45",
  "comments": 73
}
```

### Коды категорий

| Код | Название |
|-----|----------|
| health | Здоровье |
| fitness | Фитнес |
| career | Карьера |
| tech | Технологии |
| style | Стиль |
| relations | Отношения |

---

## 🎨 Кастомизация

### Изменение категорий

Для добавления новой категории нужно:

1. **В index.html** - добавить элемент навигации:
```html
<a href="#" class="navigation__link category-filter" data-category="mycategory">
    Моя категория
</a>
```

2. **В Renderer.getCategoryName()** - добавить маппинг:
```javascript
const names = {
    'mycategory': 'Моя категория',
    // ... остальное
};
```

3. **В модальных формах** - добавить опцию:
```html
<option value="mycategory">Моя категория</option>
```

### Изменение цветов

Откройте `styles.css` и измените переменные:
- `#4a9eff` - основной голубой цвет
- `#1a1f36` - тёмный цвет фона
- `#2c2c2c` - основной текст

### Добавление новых форм

Модульная система позволяет легко добавлять новые модальные окна. Пример:

```html
<div class="modal" id="myModal">
    <div class="modal__content">
        <button class="modal__close">&times;</button>
        <!-- Содержимое -->
    </div>
    <div class="modal__overlay"></div>
</div>
```

```javascript
document.getElementById('myButton').addEventListener('click', () => {
    Modal.open('myModal');
});
```

---

## ❓ FAQ

**В: Где сохраняются мои данные?**
О: В локальном хранилище браузера (localStorage). Данные не передаются на сервер.

**В: Что произойдёт, если я очищу историю браузера?**
О: Все добавленные статьи и форум-посты могут быть удалены (в зависимости от параметров очистки).

**В: Могу ли я поделиться своей статьей с друзьями?**
О: Каждый друг будет иметь свои данные. Рекомендуется экспортировать данные через консоль разработчика.

**В: Сколько статей я могу добавить?**
О: Практически неограниченно (ограничение примерно 5-10 МБ на сайт).

**В: Как вернуть удалённую статью?**
О: К сожалению, нельзя. Используйте кнопку удаления с осторожностью.

**В: Как изменить существующую статью?**
О: Текущая версия не поддерживает редактирование. Удалите и создайте заново.

---

## 📞 Поддержка

Для проблем и предложений по функциональности смотрите документацию:
- [README.md](README.md) - общая информация
- [QUICKSTART.md](QUICKSTART.md) - быстрый старт
- [CUSTOMIZATION.md](CUSTOMIZATION.md) - кастомизация
- [PROJECT-SUMMARY.md](PROJECT-SUMMARY.md) - обзор проекта
