# 🚀 ПЕРВЫЙ ЗАПУСК - ШАГ ЗА ШАГОМ

## ✅ ЧЕК-ЛИСТ

Прежде чем начать, убедись что у тебя есть:
- [ ] Node.js установлен (node -v)
- [ ] npm установлен (npm -v)
- [ ] Git установлен (git --version)
- [ ] Аккаунт на Supabase (supabase.com)
- [ ] Текстовый редактор для .env

---

## 🎯 ЭТАП 1: ЛОКАЛЬНЫЙ ЗАПУСК (15 минут)

### Шаг 1.1 - Перейти в проект
```bash
cd "/home/d1mt1m/Рабочий стол/men.ru new"
ls -la  # проверить что видишь server.js, package.json и т.д.
```

### Шаг 1.2 - Установить зависимости
```bash
npm install

# Должно вывести:
# added 123 packages in 45s
```

### Шаг 1.3 - Создать .env файл
```bash
cp .env.example .env
cat .env  # проверить что создался
```

### Шаг 1.4 - Заполнить .env (пока заглушки)
Открой .env в редакторе и убедись что есть:
```env
PORT=3000
NODE_ENV=development
SUPABASE_URL=https://placeholder.supabase.co
SUPABASE_KEY=placeholder_key
JWT_SECRET=my-super-secret-jwt-key-at-least-32-characters-long
```

### Шаг 1.5 - Тестовый запуск
```bash
npm run dev

# Должно вывести:
# 🚀 Man.ru Backend Server Started
# 📡 Server running on port 3000
# 🌍 API: http://localhost:3000/api
```

### Шаг 1.6 - Проверить health endpoint
В другом терминале:
```bash
curl http://localhost:3000/health

# Должен вернуть:
# {"status":"ok","timestamp":"2026-01-17T...","uptime":2.345}
```

✅ **Бэкенд работает локально!**

---

## 🎯 ЭТАП 2: СОЗДАНИЕ SUPABASE БД (15 минут)

### Шаг 2.1 - Зарегистрироваться на Supabase
1. Открой https://supabase.com
2. Нажми "Start your project"
3. Sign up with email или GitHub
4. Подтверди email

### Шаг 2.2 - Создать проект
1. Нажми "New Project"
2. Заполни:
   - Name: `man-ru-db`
   - Password: **Сохрани!** Например: `MySecurePassword2026!`
   - Region: Europe / Moscow (если есть) или ближайший
3. Нажми "Create new project"
4. Жди инициализации (2-3 минуты)

### Шаг 2.3 - Получить ключи

После инициализации перейди в **Settings → API**:

1. Найди **Project URL**:
   - Выглядит как: `https://xxxxxxxxxxxxx.supabase.co`
   - Скопируй (Ctrl+C)

2. Найди **anon public key**:
   - Очень длинная строка: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
   - Скопируй (Ctrl+C)

3. Сохрани где-нибудь безопасно (в текстовом файле)

### Шаг 2.4 - Обновить .env

Открой `.env` и замени на реальные значения:
```env
PORT=3000
NODE_ENV=development
SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
JWT_SECRET=my-super-secret-jwt-key-at-least-32-characters-long-2026
```

### Шаг 2.5 - Инициализировать таблицы БД

1. В Supabase перейди в **SQL Editor**
2. Нажми **"New query"**
3. Открой файл `DATABASE_INIT.sql` в редакторе
4. Скопируй **весь контент** (Ctrl+A, Ctrl+C)
5. Вставь в SQL Editor (Ctrl+V)
6. Нажми **"Run"** (или Ctrl+Enter)
7. Жди завершения

Должно быть сообщение: ✅ **Success** (внизу)

### Шаг 2.6 - Проверить таблицы

В Supabase слева выбери **"Tables"**:

Должны видеть:
- [ ] users
- [ ] articles
- [ ] forum_posts
- [ ] forum_replies
- [ ] article_comments
- [ ] notifications
- [ ] activity_logs
- [ ] subscriptions

✅ **База данных готова!**

---

## 🎯 ЭТАП 3: ТЕСТИРОВАНИЕ API (10 минут)

### Шаг 3.1 - Перезапустить сервер
```bash
# Если сервер ещё работает - останови (Ctrl+C)
# Перезапусти:
npm run dev
```

### Шаг 3.2 - Регистрация

В новом терминале выполни:
```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name":"Иван Петров",
    "email":"ivan@example.com",
    "password":"password123"
  }'
```

Должен вернуть:
```json
{
  "success": true,
  "user": {
    "id": "1705497600000",
    "name": "Иван Петров",
    "email": "ivan@example.com",
    "avatar": "👤"
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

**Скопируй токен!** Будет нужен для следующих запросов.

### Шаг 3.3 - Вход
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email":"ivan@example.com",
    "password":"password123"
  }'
```

Должен вернуть же данные + токен.

### Шаг 3.4 - Создание статьи

Замени TOKEN на токен из Шага 3.2:

```bash
curl -X POST http://localhost:3000/api/articles \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer TOKEN" \
  -d '{
    "title":"Моя первая статья",
    "description":"Это описание моей статьи о здоровье",
    "category":"health"
  }'
```

Должен вернуть статью с ID.

### Шаг 3.5 - Получение всех статей
```bash
curl http://localhost:3000/api/articles

# Должен вернуть массив статей
```

### Шаг 3.6 - Создание поста на форуме
```bash
curl -X POST http://localhost:3000/api/forum/posts \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer TOKEN" \
  -d '{
    "title":"Как начать здоровый образ жизни?",
    "message":"Ищу советы как начать бегать и правильно питаться",
    "category":"health"
  }'
```

✅ **API полностью работает!**

---

## 🎯 ЭТАП 4: ТЕСТИРОВАНИЕ ФРОНТЕНДА (10 минут)

### Шаг 4.1 - Запустить фронтенд сервер

В третьем терминале:
```bash
cd "/home/d1mt1m/Рабочий стол/men.ru new"
python3 -m http.server 8000

# Должно быть:
# Serving HTTP on 0.0.0.0 port 8000 (http://0.0.0.0:8000/) ...
```

### Шаг 4.2 - Открыть сайт

Открой в браузере:
```
http://localhost:8000
```

### Шаг 4.3 - Протестировать функции

1. **Регистрация:**
   - Нажми кнопку регистрации
   - Заполни: Имя, Email, Пароль
   - Нажми "Зарегистрироваться"
   - Должно быть сообщение "✅ Регистрация успешна!"

2. **Создание статьи:**
   - Перейди на страницу категории (например, health.html)
   - Нажми "Добавить статью"
   - Заполни заголовок и описание
   - Нажми "Опубликовать"
   - Статья должна появиться в списке

3. **Форум:**
   - Перейди на forum.html
   - Нажми "Создать новую тему"
   - Заполни данные
   - Нажми "Создать тему"
   - Тема должна появиться

4. **Профиль:**
   - Нажми на иконку пользователя (правый верх)
   - Перейди в профиль
   - Должна видеть статистику и созданное содержимое

✅ **Полный цикл работает!**

---

## 📊 СТРУКТУРА ТЕРМИНАЛОВ

**Рекомендуется открыть 3 терминала:**

### Терминал 1: Backend
```bash
cd "/home/d1mt1m/Рабочий стол/men.ru new"
npm run dev
# Выводит:
# 🚀 Man.ru Backend Server Started
```

### Терминал 2: Frontend
```bash
cd "/home/d1mt1m/Рабочий стол/men.ru new"
python3 -m http.server 8000
# Выводит:
# Serving HTTP on 0.0.0.0 port 8000
```

### Терминал 3: Команды
```bash
# Здесь выполняешь curl команды и git команды
curl http://localhost:3000/health
```

---

## 🔄 РАЗВИТИЕ ПРОЕКТА

### Как добавить новый endpoint

1. **Открыть server.js**
2. **Добавить маршрут** (после существующих):
```javascript
app.get('/api/new-endpoint', async (req, res) => {
    try {
        // твой код здесь
        res.json({ message: 'Success' });
    } catch (error) {
        res.status(500).json({ error: error.message });
    }
});
```
3. **Сохранить файл**
4. **Сервер автоматически перезагружается** (если npm run dev)
5. **Тестировать** через curl или фронтенд

### Как добавить новую таблицу

1. **Открыть DATABASE_INIT.sql**
2. **Добавить CREATE TABLE** в конец (перед ПРИМЕРАМИ ДАННЫХ)
3. **В Supabase перейти в SQL Editor**
4. **Создать новый query**
5. **Вставить только CREATE TABLE и индексы**
6. **Выполнить (Run)**

---

## 🎊 УСПЕХ!

Теперь у тебя есть:
- ✅ Работающий бэкенд
- ✅ PostgreSQL база данных
- ✅ REST API с аутентификацией
- ✅ Фронтенд, подключённый к API
- ✅ Всё готово к развёртыванию

**Следующий шаг:** Смотри **DEPLOYMENT-CHECKLIST.md** для публикации в интернет.

---

**Удачи в разработке! 🚀**
